package Angela.app;

import javafx.application.Application;

public class Launcher {
    /**
     * Main class to call for running the GUI.
     * @param args
     */
    public static void main(String[] args) {
        Application.launch(App.class, args);
    }
}
